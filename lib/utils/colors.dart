import 'package:flutter/material.dart';

const buttonColor = Color.fromRGBO(127, 17, 160, 1);
const wordLogo = Color.fromRGBO(219, 80, 0, 1);
const appBar = Color.fromRGBO(96, 50, 114, 1);
const bottomNav = Color.fromRGBO(174, 140, 187, 1);

const backgroundColor = Colors.white;
const backgroundColorDark = Color.fromARGB(255, 44, 44, 44);
const secondaryBackgroundColor = Color.fromARGB(255, 100, 96, 96);

const primaryColor = Colors.black;
const primaryColorDark = Color.fromARGB(255, 255, 255, 255);
const primaryColorGradient = Colors.pink;
const primaryColorGradient2 = Colors.pinkAccent;

const secondaryColor = Colors.blueAccent;
const secondaryColor2 = Colors.blue;

const card = Color.fromARGB(255, 107, 143, 128);
